name = "homework-helper-ai"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "src/frontend/dist"

# ─────────────────────────────────────────────────────────────────────────────
# ENVIRONMENT VARIABLES
# ─────────────────────────────────────────────────────────────────────────────
# DO NOT put real API keys here. Use Wrangler secrets instead:
#
#   wrangler pages secret put OPENAI_API_KEY
#   wrangler pages secret put GROQ_API_KEY
#   wrangler pages secret put GEMINI_API_KEY
#   wrangler pages secret put PERPLEXITY_API_KEY
#   wrangler pages secret put TOGETHER_API_KEY
#   wrangler pages secret put FIREWORKS_API_KEY
#   wrangler pages secret put OPENROUTER_API_KEY
#
# For local development, create a `.dev.vars` file (see .dev.vars.example).
# ─────────────────────────────────────────────────────────────────────────────

[vars]
# Public non-secret vars can go here (none needed currently)
